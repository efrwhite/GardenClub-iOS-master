//
//  backgroundDb.swift
//  CFGC
//
//  Created by Cory on 3/19/18.
//  Copyright © 2018 Tabor Scott. All rights reserved.
//

import Foundation

class BackgroundDb {
    
    /*
    init(String: type, String: userName, String: password) {
        let dataBaseURL = "http://satoshi.cis.uncw.edu/~jbr5433/GardenClub/login.php"
        
        if(type == "login"){
            //created NSURL
            let requestURL = URL(string: dataBaseURL)
            
            //creating NSMutableURLRequest
            let request = NSMutableURLRequest(url: requestURL!)
            
            //setting the method to post
            request.httpMethod = "POST"
            
            let task = NSURLSession.sharedSession().dataTaskWithRequest(request){
                data, response, error in
                
                if error != nil{
                    print("error is \(error)")
                    return;
                }
        }
    }
 */
}
